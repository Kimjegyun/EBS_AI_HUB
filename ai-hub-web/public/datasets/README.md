# datasets

앱에 번들되는 자산 데이터셋 위치입니다.

**실제 자산 데이터는 이 저장소에 포함되지 않습니다.** `.gitignore`가 이 폴더의 내용을
추적에서 제외하며, `sample_asset_template.xlsx`(양식 예시)만 예외로 포함됩니다.

운영 환경에서는 관리자 화면(설정 → DB 파일 관리)에서 운영관리부 전사 양식과
본부별 ERP 자산현황을 업로드해 서버 데이터셋을 생성합니다. 이 폴더에 파일을
직접 넣을 필요는 없습니다.
