{
  "name": "ebs-ai-hub-app-template",
  "version": "1.0.0",
  "private": true,
  "description": "EBS AI HUB 앱 템플릿 — JSX 로 쓰고 단일 ESM 번들로 빌드합니다",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "check": "node build.mjs --check"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
