# 기여 안내

EBS AI HUB는 **참여형 오픈소스 프로젝트**입니다. 한 조직의 내부 도구로 닫아두지 않고,
필요한 사람이 필요한 만큼 고쳐 쓰도록 공개합니다.

## 이 프로젝트가 열려 있는 이유

조직이라면 규모나 업종을 가리지 않고 비슷한 업무가 반복됩니다. 자산과 비품 관리,
일정 공유, 문서 정리, AI 보조 — 그런데 그 도구는 회사마다 새로 만들거나, 비싼 솔루션을
사거나, 결국 엑셀로 버팁니다.

EBS AI HUB는 그 공통분모를 **어느 조직에서나 도입할 수 있는 AI 업무 허브**로 만들고,
소프트웨어를 **공공재처럼 열어 함께 키우려는** 시도입니다.
가져다 쓰고, 필요한 앱을 붙이고, 고친 것을 다시 나누면 모두의 비용이 내려갑니다.
**쓰는 사람이 곧 만드는 사람이 되는 것**이 이 프로젝트가 바라는 모습입니다.

## 무엇을 기여할 수 있나

### 1. 마켓플레이스에 새 앱 등록하기 — 가장 환영합니다

앱을 넣는 길이 두 가지입니다.

**(A) 저장소에 등록 — 널리 쓰일 앱**
앱을 만들어 PR로 등록하면 모두가 내려받아 마켓플레이스에서 설치해 쓸 수 있습니다.
이 프로젝트의 앱은 **파일 하나 + 레지스트리 한 줄**입니다.

```
1. ai-hub-web/src/apps/yourApp.tsx 를 만들고 AppPlugin 을 export
2. ai-hub-web/src/apps/registry.ts 에 import 하고 APP_REGISTRY 에 추가
```

대시보드·마켓플레이스·설치 관리 화면에 자동으로 올라옵니다. 셸 코드는 건드릴 필요가 없습니다.
계약은 [`src/apps/types.ts`](ai-hub-web/src/apps/types.ts) 의 `AppPlugin` 인터페이스를 보세요.

**(B) 원격 앱으로 배포 — 조직 전용 앱이나 빠른 실험**
번들(JS 한 파일)을 관리자 화면에서 올리면 **허브를 다시 빌드하지 않고** 실행 중에 등록됩니다.
빌드 도구도 필요 없습니다. 규격과 예제는 [doc/REMOTE_APPS.md](doc/REMOTE_APPS.md) 를 보세요.

만든 앱은 **파일 하나로 내보내 다른 회사 허브에 붙일 수 있습니다.** 사내에서 만든
앱이라 저장소에 PR 로 올리기 어렵다면, 내보낸 파일을 공유하는 것만으로도 나눌 수 있습니다.

### 2. 자기 조직의 실사 양식 지원 추가

현재 재물조사 앱은 방송장비 자산 대장 구조에 맞춰져 있습니다.
전산장비, 도서, 실험기자재, 의료기기 등 **다른 자산 유형의 열 구조**를 지원하는 기여를 환영합니다.
열 매핑은 [`excelService.ts`](ai-hub-web/src/apps/inventory/excelService.ts) 한 곳에 모여 있습니다.

### 3. 로컬 모델 연동 확장

AI 보조 기능은 OpenAI 호환 규격을 쓰므로 Ollama·llama.cpp·vLLM이 바로 붙습니다
([doc/LOCAL_MODEL.md](doc/LOCAL_MODEL.md)). 다른 런타임이나 임베딩·음성 모델 연동도 좋은 기여입니다.

### 4. 버그 신고 · 문서 개선 · 번역

현장에서 겪은 문제를 이슈로 남겨주세요. 특히 **다른 조직의 데이터에서 깨지는 경우**가
가장 값진 제보입니다. 이 프로젝트는 "자산번호에 하이픈이 있고 없고"처럼
실제 데이터를 열어봐야만 발견되는 문제로 만들어졌습니다.

## 개발 규칙

### 앱을 만들거나 고치면 문서도 함께

**모든 앱은 [`doc/apps/`](doc/apps/README.md) 아래에 문서를 하나 가집니다.**
실행 순서, 화면별 메뉴얼, 데이터 저장 위치, 알려진 한계를 담습니다.
기능을 바꾸면 **같은 PR 안에서** 문서도 고쳐주세요. 템플릿은 [`doc/apps/TEMPLATE.md`](doc/apps/TEMPLATE.md) 입니다.

### 절대 커밋하면 안 되는 것

- API 키, 토큰, 비밀번호 — `.env*` 는 전부 `.gitignore` 대상입니다
- 실제 자산 데이터, 개인정보, 사내 문서
- `server/data/`, `server/uploads/`, `server/backups/`, `supabase/.temp/`

PR을 올리기 전에 `git diff --cached --name-only` 로 한 번 확인해주세요.

### 코드 스타일

- TypeScript strict. `npm run build --prefix ai-hub-web` 가 통과해야 합니다
- `npx eslint src` 로 새 경고를 만들지 않는지 확인
- 주석은 **왜 그렇게 했는지**를 적습니다. 무엇을 하는지는 코드가 말합니다

## 기여 절차

1. 이슈로 먼저 논의해주세요 (특히 큰 변경)
2. 포크 → 브랜치 → 커밋
3. PR 설명에 **무엇을 왜 바꿨는지**와 **어떻게 확인했는지**를 적어주세요
4. 문서 갱신이 포함됐는지 확인

## 라이선스

기여하신 코드는 [MIT License](LICENSE) 로 배포됩니다.
